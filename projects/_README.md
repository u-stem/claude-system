# projects/

各プロジェクトの統合情報(プロジェクトメタデータ、fragment 適用記録、生成された CLAUDE.md など)を格納する。

## 注意

このディレクトリの**中身は `.gitignore` で除外**されている(プロジェクト固有の情報には機密が含まれうるため)。

ディレクトリ構造を残すために `.gitkeep` と本 `_README.md` のみを git 管理する(ルート `.gitignore` の除外指定参照)。

詳細は Phase 8 で整備される。
